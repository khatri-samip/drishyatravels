from django.db import models

# TODO: [Medium/Code Quality] Empty Django models.py
// The trip_planner Django app has no models defined. The trip planner API was
// migrated to PHP (backend/api/trip-planner/) but the Django app structure remains.
// Either: (a) Remove this Django app entirely, or (b) Implement proper Django models
// if planning to use Django ORM. Current state is confusing dead code.
